#include <stdio.h>
/**
 * main - print alphabet in lowercase
 * Return: 0 for success
 */
int main(void)
{
	char ch = 'a';

	for (ch = 'a'; a <= 'z'; ch++)
	{
		putchar(ch);
		putchar('\n');
	}

	return (0);
}
