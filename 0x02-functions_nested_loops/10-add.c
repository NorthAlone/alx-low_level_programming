#include <stdio.h>
#include "main.h"

/**
 * add - entry point
 * Description: adds two integers and returns the result.
 * Return: sum off a and b
 * @a: First integer
 * @b: Second integer
 **/
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
